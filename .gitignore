__pycache__/
*.pyc
.venv/
*.egg-info/
build/
dist/
.pytest_cache/

# webfonts fetched by tools/fetch-fonts.py — Apache-2.0, but a script can
# get them, so they don't belong in the repo. admin.css degrades without them.
/src/memai/webui/fonts/

# local memory store (user data, not repo content)
*.db
*.db-wal
*.db-shm
/data/

# local Claude Code config
/.claude/

# design-review snapshots written by the impeccable skill — a local record of
# what was found and when, not repo content
/.impeccable/
