{
  "$schema": "https://raw.githubusercontent.com/googleapis/release-please/main/schemas/config.json",
  "packages": {
    ".": {
      "release-type": "python",
      "package-name": "memai",
      "bump-minor-pre-major": true,
      "include-component-in-tag": false,
      "extra-files": [
        { "type": "generic", "path": "src/memai/__init__.py" }
      ],
      "pull-request-title-pattern": "chore: release ${version}",
      "pull-request-header": "Everything that landed on `dev` since the last tag. Merging turns it into a MemAI release.",
      "pull-request-footer": "The version comes from the conventional commits since the last tag -- a `fix` moves the patch, a `feat` the minor, and below 1.0.0 a breaking change moves the minor too."
    }
  }
}
