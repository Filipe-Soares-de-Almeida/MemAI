# npm treats "engines" as advice unless this is set. install.bat and CI both
# run `npm ci`, so a Node below the declared floor stops there with a message
# instead of somewhere inside the build.
engine-strict=true
